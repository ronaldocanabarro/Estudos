// VARIÁVEIS

let idade = 26;             // Criando a primeira variável

console.log(idade)

// Tipos de variáveis

idade = 30;                 // Number
let numero = 30;            // Number
let altura = 1.77;          // Number 
let nome = "Walisson";      // String (texto)
let estudando = true;       // Booleano (true ou false)

console.log(numero, typeof numero);
console.log(altura, typeof altura);
console.log(nome, typeof nome);
console.log(estudando, typeof estudando);

let outra;                  // Undefined
console.log(outra);

let nula = null;
console.log(nula);          // Null

var variavel = 30;          // Sintaxe antiga no JS, não recondável a utilização

const constante = 100       // Uma variável constante não recebe nova atribuição